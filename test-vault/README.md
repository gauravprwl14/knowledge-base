# KMS Test Vault

This is the test Obsidian vault for KMS development.

Add your own files here to test the full document processing pipeline:
- Markdown notes (`.md`)
- CSV files (`.csv`)
- PDFs (`.pdf`)
- Images (`.png`, `.jpg`)

The scan-worker will detect changes between scans using SHA-256 checksums.
